spring:
  application:
    name: litchi-sidecar
server:
  port: 8070
eureka:
  client:
    service-url:
      defaultZone: http://root:root@localhost:8761/eureka
sidecar:
  port: 8000
  health-uri: http://localhost:8000/health.json 