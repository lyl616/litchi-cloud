server:
  port: 8181
spring:
  application:
    name: litchi-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/startSnow/open-cloud
          search-paths: /src/main/resources/
  profiles:
    active: prod